{
  "name": "prisma-starter",
  "version": "0.0.1",
  "description": "",
  "main": "./dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "dev": "tsnd --respawn --transpile-only src/index.ts",
    "watch": "tsc --watch",
    "check": "tsc --noEmit"
  },
  "prisma": {
    "seed": "ts-node prisma/seed.ts"
  },
  "devDependencies": {
    "prisma": "^3.10.0"
  },
  "dependencies": {
    "@prisma/client": "^3.10.0"
  }
}
